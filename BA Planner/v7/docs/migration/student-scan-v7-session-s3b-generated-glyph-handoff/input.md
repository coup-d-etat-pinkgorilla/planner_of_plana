# S3B generated glyph continuation input

- 기록된 S3B 생성형 glyph template 실험을 시작한다.
- v6의 background + equipment icon + rendered number 경로에서 text layer만 분리한다.
- outline-only, fill+outline, fill-only와 기존 menu binary를 같은 frozen 답지에서 비교한다.
- 실제 한 자리 숫자를 고정 두 cell로 잘라 판독하지 않는다.
- 결과는 shadow evidence로만 통합하고 production enablement와 S4/S5는 변경하지 않는다.
- benchmark, portable fixture, tests, Almanac과 handoff를 함께 갱신한다.
