# Mermaid browser bundle

- Package: `mermaid`
- Version: `10.9.6`
- Source: https://github.com/mermaid-js/mermaid
- License: MIT (`LICENSE.txt`)

`mermaid.min.js` is the package's browser bundle. UCS uses it locally through
QtWebEngine so UI behavior Markdown documents render without a network request.
